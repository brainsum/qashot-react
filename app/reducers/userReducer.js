export default function reducer(state={
  user: {
    id: null,
    userName: null,
  },
  fetching: false,
  fetched: false,
  error: null,
}, action) {

  switch (action.type) {

  }

  return state;
}
